export const config = {
  botName: process.env.BOT_NAME || "NEXUS",

  ownerNumber:
    process.env.OWNER_NUMBER || "628xxxxxxxxxx",

  prefix: ".",

  version: "1.0.0",

  sessionFolder: "./session"
};
